#  Screens

This folder contains all screens of our application. With a simple glance at this folder we can see all screens within our application.

## What Counts As a Screen?

* A subpage of a `TabView()`
* The main page of a `NavigationView`
* All subpages of a `NavigationView`
* A presented `.sheet { ... }`
